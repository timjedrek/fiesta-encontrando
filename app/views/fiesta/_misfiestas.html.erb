<!--Fiestas Pasadas-->
<div class="bg-white px-2 md:px-10 md:pb-10 md:pt-5 ">
  <!--Actual feed content-->
  <div class="flex flex-col items-center pb-10" >
    <% current_user.created_fiesta.reverse_each do | fiestum | %>
      <%= render "bubble", fiestum: fiestum%>
    <% end %>
  </div>
</div>